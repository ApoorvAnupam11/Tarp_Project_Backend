# Tarp_Project_Backend
resource pooling app


clone the repo and do changes.
plz attach the dependecies info and functionality description with each commit (each file, if possible)
 also the server is set to run by "npm start"
 and is hosted on localhost 8000, make sure nothing is running on it.
